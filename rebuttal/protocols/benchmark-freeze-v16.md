# Strict benchmark freeze, version 16

Freeze date: 2026-09-05. This architecture-transfer registry is frozen before any target-repository Mistral, OLMo-2, or Granite model output.

The candidate is method version 10's fixed deployment-aligned posterior spherical steering. Model revisions, layers, fit construction, candidate configuration, matched-Spherical grid, prompt, scoring, 300/160/120/50/187 allocation, and screening rule are frozen in method version 10.

For each family, materialize representation-fit artifacts and all observer outputs first. A family that fails the observer screen stops without reading its 120 development or 237 confirmatory groups. A passing family skips the unused 120 development groups and runs candidate plus matched Spherical once on the 50 ID and 187 shifted groups. This preserves a 237-group confirmatory sample per architecture.

Per-family paired calibrated-MC2 and MC1 BCa intervals use 10,000 resamples and seed 20260905. Across three families, Bonferroni two-sided alpha is `0.05/3`. Each family requires calibrated-MC2 lower bound above zero and MC1 lower bound above -0.015. ID and shift strata are reported separately. Five exact update-norm isotropic controls, no-op, and the unprompted/last-token fit ablation are run only after the candidate beats matched Spherical in that family.

The architecture claim requires all three families. Any failed or stopped family remains in the aggregate and cannot be dropped. Multi-behavior, capability, specificity, and collateral gates remain required before SOTA wording.
