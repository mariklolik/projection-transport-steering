# IRC real-question runtime protocol

2026-09-07. Prospective engineering increment, before real-question generation. This executes the existing 16-question runtime dependency, not a new behavioral hypothesis, benchmark replacement or generation-cap rescue. The prior goal turn is classified as progress: immutable data admission, independent reconstruction and CUDA integration changed the authoritative state and next action.

## Decision and invariant boundary

Can the admitted isolated engine execute the fixed math prompt, evaluation and same-prefix branch contracts on real questions, with complete immutable output accounting and a usable measured cost envelope? A missing row, duplicate identity, scoring exception, changed source hash, failed identity/split replay or parent-cache mutation fails this engineering increment. Wrong, unboxed, empty and budget-exhausted model outputs are retained scored outcomes, not source-gate failures. Their quality does not select a method or change this protocol.

Reuse the allocation manifest `e489f1273b1c667ac5f5bbc35963a5ed480a01224f155cf52ee56bbd79083156` and its fit packet `2585bc810dc0089f342a1e578f37f4372f9c103090be0bba1932c56fdca6e4eb`. Select 16 fit groups with existing `allocate_groups`, quota `runtime:16`, salt `irc-20260907-runtime`; retain the returned hash order. Copy only these fit rows into the runtime packet. No response, selection, development or test payload is mounted in a runtime worker. Use the existing `rollout_seed(cluster_id, 0)` consistently across batch conditions.

## Fixed workloads

Use Qwen3-8B at the already pinned revision, native chat template with `enable_thinking=True`, and one user message: `Solve the following problem. Reason step by step, and put your final answer within \boxed{}.\n\n` followed by the exact source problem. No system message or synthetic answer-format suffix is added. Tokenize without truncation or an extra special-token pass; pad on the left. The inspected native template ends at the assistant role marker when thinking is enabled; it does not itself append an opening thinking tag.

1. Profile the same 16 questions at microbatches 1, 4 and 8, each with 512 generated-token steps. For each batch, time one reference continuation, verify a full identity-hook replay, verify split/resume at 256 steps, and apply a fixed nonzero additive action for 64 steps from that cut solely to verify parent-cache isolation. Persist all reference outputs and check-path tokens/hashes. No score is used for batch selection.
2. Run all 16 questions once at batch 8 under the unchanged 8,192-token research budget, to exercise real termination, output scoring and long-context memory/cost. This is a separately named workload; do not compare its timing directly with the 512-step profiles. It does not repeat the three short-profile replay paths at full length.

Sampling remains temperature 0.6, top-p 0.95, top-k 20, model-declared EOS/pad, bfloat16 and SDPA. Two intra-op CPU threads, one inter-op thread and per-row generators are fixed. Four independent resident workers may run these four conditions concurrently after checking idle GPUs. Each worker loads weights once and processes the fixed consecutive batches. Peak allocation includes the actual replay/branch workload; reference timing excludes replay checks and grading, which are reported separately from full-process cost. The first measured batch includes first-workload overhead; no repeated-run speedup or confidence interval is inferred from one execution.

The short workload is a prospective throughput/conformance instrument, not a reduction of the scientific generation budget. Only the 8,192-step workload characterizes full-budget task-output accounting. The total existing runtime-stage ceiling stays one H100-hour, including earlier engineering work where measurable; new launch timeouts sum to at most 0.80 H100-hours, reserving the remainder for known prior checks, deployment and measurement overhead. Per-condition caps are profile-1 1,200 seconds, profile-4 450, profile-8 330 and full-budget-8 900. A timeout retains the partial packet and fails completeness; it is not permission to change sample size or claim throughput from completed rows alone.

## Scoring and reproducibility

Apply the already amended primary author-normalized score exactly once to every reference output. Apply unchanged Math-Verify exactly once when its reference-support flag is true; otherwise mark sensitivity unsupported without changing the primary denominator. Catch explicit Math-Verify `TimeoutException` plus ordinary exceptions, retain the exception class, and fail technical admission after writing the output. No retry or re-generation is permitted to repair a grade. Replay and cache-check continuations are engineering outputs, not additional task replicates.

Store raw reference tokens and decoded completions, row/group IDs, seed, prompt/token hashes, termination/length, each evaluator result or exception, batch identity and seconds. Store replay and intervention tokens separately. Persist each completed batch with exclusive creation; append-only chunk files survive later failure. Write a final receipt only after exact expected-ID equality, uniqueness, source/model/config hashes and every technical check pass. A failed final receipt must retain its reason. An interrupted process without a final receipt is incomplete, not passed. There are 64 declared scored rows across the four workload conditions, but only 16 independent questions and no confirmatory method comparison.

Before launch, record the immutable container image, Python/platform, the exact imported package distribution inventory and fingerprints of the active environment, overlay, model/tokenizer and result-generating source files. This is an explicit scientific-engine candidate qualification, not an assertion that the remote host installed the current local `uv.lock`. The native Torch 2.13 failure remains closed; no inference policy is changed to accommodate it.

## Discovery before code

`branch_runtime.py` already owns per-row generators, copied pending-token/cache state, absorbing EOS and isolated continuation; call it unchanged. `TorchLayerAction` and `AdditiveAction` already provide the exact tested hook contracts. `outcome_score_runtime.rollout_seed` supplies stable identities. The existing old sentinel runner is single-row/AIME-specific and must not be adapted because that experiment is closed. `run_irc_branch_probe.py` is a frozen synthetic probe; preserve its source hash and receipts.

The one missing executable is a real-question batch runner wiring the admitted packet and those existing functions together. Put the thin CLI in `scripts/run_irc_real_runtime.py`; extend the existing `outcome_score_runtime.py` with the math batch helper and `outcome_score.py` with the combined diagnostic-scoring wrapper. No old function body changes. The hash helper is passed as a callable rather than reimplemented or imported from a script into library code. Reuse `load_math_author`, `file_sha256` and `payload_sha256`. The caller supplies a hash-bound config and one declared condition; the output is an exclusive directory of row/check chunks and a final receipt. Tests first cover real tiny-model execution, packet/hash/identity rejection, repeated-output protection, reference/sensitivity exception accounting and exact expected-ID joins. No generic execution framework is added.

## Research and paper scope

Current contribution evidence remains `EVIDENCE_THIN`: S5 effect/uncertainty and S7 strong-comparator execution are absent despite improved S3/S4/S9 measurement and provenance. Closest-work boundaries and the finite IRC action family remain those of the unchanged reset synthesis. This engineering increment can resolve runtime uncertainty but cannot promote C67 or substitute for the fixed-adapter entry condition, response pilot, full-rollout development, simultaneous strong-comparator tests, multi-model/behavior replication or paper review.

While the runtime workers execute, inspect only training-side source lengths and the pinned ReFT training recipe to prepare the next fit/selection contract. The exact training schedule and native reasoning/answer target format must be frozen before baseline fitting; no real-runtime correctness observation is a basis for selecting it. Retain every failure and ask an independent empirical critic to audit the finished runtime packet before opening baseline fitting.

## Independent prelaunch corrections

Before any GPU output, the independent critic reproduced loss of an already completed reference if a subsequent replay/check raised. The implementation now exclusively writes a raw `reference-NNNN.json` checkpoint before optional checks and before grading. A later exception retains that checkpoint and the final failure receipt inventories every saved reference file separately from fully completed/scored chunks. No reference is regenerated or graded again to repair that failure. Tiny-model fault-injection tests cover this order directly.

The critic also demonstrated that all rows may terminate before the intervention cut. Preserve their absorbing transitions, and record the number at risk, per-row additional action tokens and action forward calls, derived from continuation width under the audited one-forward-per-step contract. A zero-at-risk nonzero path is `not_exercised_absorbing`, not positive evidence that an intervention executed. Do not replace those questions. Overall reporting distinguishes this coverage from identity/split replay and the previously passed non-absorbing synthetic action check.

Launch four named detached containers, each with its own in-container `timeout`, so an SSH observation failure cannot restart or orphan a scientifically duplicated worker. The timeout includes Python imports and the full runner process; its 20-second kill grace is separately reserved in the one-H100-hour stage envelope. Retain containers and Docker logs until their terminal state, output hashes and start/finish timestamps have been collected. Clean up only these exact created containers after preserving their evidence. Poll the same container IDs after observation failures and never infer process death from an SSH timeout alone.
