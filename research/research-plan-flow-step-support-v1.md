# Flow-step support research plan, version 1

Freeze date: 2026-09-07. Status: completed and closed at the post-hoc mechanism sentinel.

1. Preserve all earlier route closures and the complete ResidualFlowBack failure packet.
2. Freeze the FLAS, ODE, dynamic-gating, and released-analysis ownership boundary before output.
3. Extend only the existing released-FLAS context adapter to expose the already implemented `_iv` step-selection and net-scale state with guaranteed cleanup.
4. Implement the all-subset evaluator and prospective concept-level analyzer; verify exact Shapley reconstruction and the fixed gate with synthetic tests.
5. Run one concept on one H100. If replay, condition enumeration, forward count, or finite-output checks fail, repair only infrastructure and preserve invalid attempts.
6. If the smoke is valid, execute six two-concept shards on six H100s and run the frozen analyzer before inspecting condition-level results.
7. Close on failure. On a pass, freeze the sole candidate and only then materialize the next 24 concepts for a fresh development test.

This route changes the support of the released vector field. It does not rescue ResidualFlowBack by changing a quantile, additive dose, witness mapping, metric, or safety cap. Claim and A-star gates remain blocked until fresh behavior evidence exists; the strengthening gate passes only for this low-cost diagnostic increment.

## Outcome

The 12-concept all-subset sentinel completed, but the fixed candidate failed FS.3 and FS.4. No fresh development concept, generation, judge call, pilot, held-out concept, or larger model was opened. The complete decision is in `research/flow-step-support-design-audit-v1.md`.
