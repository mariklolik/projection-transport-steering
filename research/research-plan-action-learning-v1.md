# Action-learning research increment after the complete baseline failure

Started 2026-09-08. Design/source investigation, not a new GPU protocol or submission prose.

## Decision and immutable boundary

The complete original baseline-v3 family fails useful-action entry: best full133/256 versus zero198/256,8rescues/73harms. Closure SHA256 `17b8ded150c916b45052412838e05a71c932e38807924205c2b73e6e686eef96`; native result `03433c5dc63d9f8ec16308e34a58754eccfb8cc36078d0b1c54a08c584aa8467`. This is progress from the previous goal turn, not an external blocker. Preserve all prior failures, exact original thresholds, cap, split/exposure boundaries and accumulated costs. No response collection or new vector/strength library on the failed family.

Question: can a genuinely different, base-policy-aligned learning objective yield a useful representation intervention with protected competence, and what strong information/parameter/resource-matched adaptation would distinguish scientific value from ordinary reward fine-tuning?

Audience: research collaborators and skeptical empirical-method reviewers. Scope: the exact action/learning/comparator bottleneck, nearest primary steering/adaptation methods within the existing2022–present corpus, updated through the current query date. Not a second broad survey or an assertion of novelty. Source classes: original papers and author code, official model/data documentation, public OpenReview reviews where available; alphaXiv is discovery and primary-content retrieval, not authority for generated claims.

Assumptions: the target is `mariklolik/projection-transport-steering`; source checkout remains read-only; heavy execution stays on FSK. Existing source/fit/zero/selection audits are reusable only at their matching hashes. The full three-architecture/three-construct empirical SOTA and ICLR-ready goal is unchanged. Neither target-distribution mismatch nor a solution is yet causally established.

## Ordered work

| Step | Status | Decision output |
|---|---|---|
| Targeted nearest-neighbor discovery and source-gap reconciliation | complete | Four nearest primary methods selected: RISER, PGS, OGLS-SD and first-token REFT. Reward/KL, Fisher calibration and outcome-conditioned self-distillation are prior art, not candidate novelty |
| Follow up consequential source/code gaps | complete within bounded scope | RISER publication, partial release and budget discrepancy independently spot-checked; PGS objective verified; OGLS cost appendix inspected; reusable code contracts audited. Unlocated or challenge-gated reviews and missing implementations stay explicit |
| Synthesize one minimal discriminating research design | complete as design, not executable protocol | Offline DPO/positive-only imitation by representation/LoRA. Common admitted questions and positive traces required; objective and coverage confounds explicit. Native collector scenario exceeds remaining allowance, so batching qualification comes first |
| Verify design/claims and record handoff | complete | Final report179344eb0855eb4a7ce226a76f200b942abd47a15e7ed92ac231a4dad884d235 passes independent AI design-consistency review; five-page PDF checked on every page, six source links retained. Scientific execution remains unadmitted |

The Deep Research `update_plan` tool is not exposed after tool discovery; this file is the planning fallback and keeps one step in progress.

## Gap matrix before discovery

| Claim family | Current evidence | Missing decision evidence | Next targeted source |
|---|---|---|---|
| Short public-solution imitation harms the base reasoning distribution | Large shortening, lower online loss and complete adverse accuracy; prior source-length warning | Objective-versus-capacity/action confounding; no causal comparison | Closest on-policy/reward-trained activation and adaptation papers |
| A useful action can be learned under task reward | Existing RISER/LRS/ACTS synthesis; no admitted useful action here | Exact reward objective, action timing/parameterization, protected baseline and released implementation | Primary methods/appendices and author source, not abstracts alone |
| Proposed increment could add scientific value | Repeated intervention-response control remains unmeasured | Closest method already implementing the same object; strongest matched comparator | Targeted novelty and code audit |
| The next complete experiment can fit allowed resources | Measured prior baseline23.053432/48H100h; no new allocation | Full data collection/fit/tuning/evaluation cost and parity for the chosen design | Existing measured receipts plus source-bound workload arithmetic, not guessed speedup |

## Prospective decision rules for this design investigation

Stop broad discovery after the initial relevant set; expand only for a consequential unresolved objective, comparator or novelty gap. Reuse already verified papers without fetching them again unless the specific needed passage or current release is missing. Record inaccessible review/code evidence as missing, not as a defeated method.

The output must distinguish established source facts, code-confirmed contracts, existing empirical observations and new hypotheses. If the nearest source already owns the proposed method, use it as a comparator or reject the novelty claim. Before any new outcomes, require exact target provenance/training-only information, code/config/data hashes, comparator budget, independent unit, seeds, primary quality and retention estimands, uncertainty/multiplicity, success/failure/ambiguity thresholds, exclusions/recovery and inclusive costs. An incomplete design does not authorize a GPU screen.

No manual labels are planned. If genuinely necessary later, the user's subagent-labeling instruction applies under a separate immutable labeling contract. No change to previously exposed selection or sealed-test identity is implied.

Skill route: research-paper-writing → scientific-claim-evidence → a-star-conference-paper contribution audit → scientific-research-strengthening, with Deep Research for source work. The completed result audit is reused at its unchanged hash. Fair-strong remains unpassed; paper writers, skeleton/global paper review and compliance are not reached. Author approval and current venue/year/track disclosure remain unverified.

## Completed increment and next goal dependency

This Deep Research increment is complete within its stated source/design scope. It produced a five-page PDF, canonical report, primary retrieval packet and claim/source ledger, preserved all historical negative evidence, and resolved five independent design critiques. It does not complete the active empirical SOTA goal.

The next goal turn should implement only a predeclared, bounded fit-only base-source collection qualification. First inspect the pinned available SGLang source/API and reuse the current data, token, seed, scoring and receipt contracts where valid. `action-learning-v1/runtime-inventory.json` records FSK42 image sha256:edc40b8cd5d95f053f05b0f7807e256038cd604c08aee981535c8e44d72180cb; metadata reports SGLang0.0.0.dev1+g20621aa14.d20260827, Torch2.13.0+cu129 and Transformers5.12.1. This was a network-disabled CPU-only metadata container, not a GPU/model/runtime pass. No batch-size or seed-determinism guarantee is inherited.

Freeze exact fit-only IDs, sampling/request seeds, raw-token output contract, image/model/tokenizer hashes and a complete engineering ceiling before CUDA. Test only new source-policy and objective contracts; do not replay unchanged grader, mask or old-family gates. Require complete source, full-length paired-policy backward/frozen-reference forward, four-arm fit and evaluation affordability before scientific dispatch. Keep prior resource totals and all response/development/sealed boundaries unchanged. Do not reopen the literature search, OpenReview challenge or failed vector family without a new consequential reason.
