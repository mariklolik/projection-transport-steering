# Isolated eager-mask engine qualification v7

## Decision, evidence and authority

The previous goal turn was progress: v6 changed authoritative evidence, passing a paired native mask-construction counterfactual and an observer-free original 35-shape/175-comparison operator matrix. Its closure SHA256 is 03bab96dce5175457eed65865862a3a104952c7a4ecbb19bbc9b639cf38f8e64. All 35 bound files matched at this turn's start. No useful baseline, actual-weight engine or SOTA was admitted.

The full empirical steering objective remains three architectures and three constructs, strong applicable comparators, uncertainty, stratification and mechanism/protection evidence. The completed literature/SOTA/benchmark reset is reused at unchanged hashes. This increment introduces no new steering novelty claim or external-source freshness claim. Its reviewer-relevant question is whether the mask-only policy can be bound at the actual model path without invalidating native masks, logits, caching or branch replay.

The existing autonomous FSK authority provides no user-imposed numerical ceiling. Reserve 3,300 container-seconds from the already recorded additional-engineering remainder of 3319.667815011 seconds. This is an assistant planning allocation, not new funding or a transfer from baseline. The old 9.198053592-second engineering remainder and baseline-v2's 7.605374254722222 H100-hours stay separate.

## Discovery before code

- Existing functionality: `attention_backend.fp32_prefill_flex_v4` delegates to the original FP32 prefill wrapper with the native short-prefill kernel option. HF `flex_attention_mask` already implements causal/custom predicates, padding and offsets. Its pinned body has no usable public compile override: its unused kwargs cannot override the final native builder's compile flag.
- Reuse: construct an isolated function binding from the unchanged native function code/defaults/closure/keyword defaults and a shallow copy of its globals, replacing only the final `create_block_mask` delegate with `_compile=False`. Do not toggle the HF Torch-version flag, duplicate predicate logic, edit generated kernels, or mutate HF/Torch shared global bindings. This is pinned-source compatibility wiring, not a new mask algorithm.
- Integration: register `fp32_prefill_flex_eager` with the unchanged v4 attention callable and isolated mask callable. Preserve both old registrations. Extend only the existing real-runtime registration condition needed for this alias. Existing C runner, data validators, tokenizer, branch runtime, scorer, hashes and output conventions remain reusable.
- Missing part: a bounded actual-prefill probe must stop after the existing model first-forward contract, without sampling or scoring. Reuse `checked_packet`, existing hash functions and the native tokenizer/model; verify its kwargs against `branch_runtime.advance_branches` instead of refactoring the generation engine or constructing another sampler.
- Caller contract: left-padded inputs/masks, cumsum positions clamped at zero, no initial cache, `use_cache=True`, `logits_to_keep=1`, eval/no_grad, native cache behavior and immutable base-parameter versions. Preserve prebuilt 4D/BlockMask bypass; do not claim that an externally supplied mask was rebuilt eagerly.

The isolated native code/defaults/globals contract, effective False compile argument, old-global identity after success/error, actual `create_causal_mask` registration path, cached offsets, prebuilt-mask bypass and idempotent alias registration require CPU tests observed RED before implementation. Native CPU tests and independent source/argv criticism precede GPU dispatch.

## Prospectively ordered qualification

| Stage | Exact workload | Inclusive container ceiling | Admission |
|---|---|---:|---|
| A | Original 35 ordered geometries / 175 unchanged FP64 comparisons through actual new registry bindings | 300s | Requires source/binding/CPU review and idle hardware |
| B | All original 32 selection-prefill batches, native Qwen3-8B tokenizer/weights and production first-forward kwargs | 600s | Only after complete A pass and integrity review |
| C-profile | Original 16 short-prompt fit-only questions, batch8, 512 steps, original branch/action/cache checks | 400s | Only after complete B pass |
| C-full | Same 16 questions, batch8, complete 8192-token condition and original scoring/terminal checks | 2000s | Only after complete C-profile pass |

Child deadlines are 260/560/360/1960 seconds, each with ten seconds of kill grace and thirty seconds startup/shutdown margin inside its container ceiling. Sum of maximum lifetimes is 3300 seconds, leaving 19.667815011 seconds unreserved. Actual compilation, loading, failed work, serialization and shutdown are charged. CPU analysis and transport are separate and are not misreported as H100 busy time.

Use one freshly verified idle FSK42 H100 at a time, the pinned image/runtime, independent initially absent caches and new output/container names. No unrelated process is stopped. No numerical, compiler, resource or timeout retry is allowed. At most one packaging-only correction may be separately recorded and independently checked before any container creation or numerical exposure; preserve the failed argv and use the same reservation. An SSH observation timeout is not a terminal container and never authorizes a duplicate launch.

A is required again because the effective binding changed from the diagnostic shared-global context to the durable isolated function. Obtain both mask and attention callables from the actual new registries. The unchanged helper directly names its mask global and attention attribute: the bootstrap must bind those exact call sites in its fresh helper namespace to the resolved callables and assert their identities, not merely pass an alias string that the helper never uses. No early/late tensor observer. Preserve original helper code, all ordered lengths, seed20260908, B8/H32/KV8/D128, BF16 inputs/FP32 prefill, FP64 reference, both forwards, QKV VJPs, rtol0.005/atol0.0005, predicates, finite gradients and masked-query zeros. One missing/failing call closes subsequent stages.

B source identities and exact prompt/input/mask hashes come from `artifacts/engineering/irc_short_prefill_v4/cpu/inputs-v4.json`, SHA256f38bb310ad38367c7cefff6c8eaf199c88525eac275d73595ba20064cc0f7edf. Use all32 batches in that stored source order. Require finite last-valid logits, exact frozen dimensions/hashes, unchanged model versions and native mask registration at the actual model. Return no generated answers, scores or selected candidate.

C reuses that same immutable input inventory's `fit_runtime_packet`: first16 fit records with 1 < native prompt length < 128 in original order, chosen before outcomes. Freeze the materialized packet/config hashes before dispatch. C-profile and C-full retain the existing runner's source/seed/EOS/replay/cache/action/scoring checks, source model revision and native engine precision. Complete raw/scored rows are necessary, including cap failures; do not change the cap or require correct answers to relabel a technical pass as scientific efficacy. Separate processes avoid reinitializing Torch interop threads inside an already active process.

## Research interpretation and boundaries

This is exploratory engine qualification, not a steering comparison, architecture replication or population efficacy estimate. Report every planned, attempted, failed and missing stage/cell; assertion counts are not independent replicates. No confidence interval on model-quality improvement is estimable here. Prefill shape metadata is previously exposed source information; C outcomes are fit-only engineering data, not selection or sealed-test evidence.

A pass of every stage plus independent integrity/claim review supports a separately scoped engine-admission decision. It does not authorize a baseline rerun automatically. A complete eight-candidate baseline requires its own explicit successor resource/source/selection contract; archived survivors are not eligible substitutes. Preserve all earlier scientific failures, v4 failure, v6 scopes and the full SOTA target.

Route: research-paper-writing → scientific-claim-evidence → scientific-results-and-figures → scientific-research-strengthening; experimental-setup-writer specifies this prospective design. Non-Superpowers TDD and modern-python govern code, with no dependency migration. This research protocol is the implementation plan under the autonomous/no-Superpowers mandate.

Current target remains EVIDENCE_AVAILABLE / EVIDENCE_THIN, S1–S9 = 3,2,0,3,0,0,0,4,3 (15/36), with input-matched anchors in v6 closure/audit and the unchanged reset benchmark/literature. Presentation is separate (3,3,4). Fair-strong and paper-prose gates remain blocked. Responsible-author verification, AI disclosure classification and ICLR compliance remain unverified. Append all attempts and model assistance; final receipts must distinguish observed evidence, critic interpretation and next-step recommendation.
