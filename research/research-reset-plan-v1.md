# Research reset: evidence, competitors, and the next decisive iteration

Requested: 2026-09-07. Target repository: `mariklolik/projection-transport-steering`.

8 September objective-learning follow-up: the [new research plan](research-plan-action-learning-v1.md) completes a bounded four-method primary-source/code investigation and selects an offline DPO/positive-only-imitation by representation/LoRA qualification. Its [PDF](../output/pdf/action-learning-research-plan-2026-09-08.pdf) preserves the adverse baseline, rejects reward/KL/Fisher-only novelty, and puts measured batched source-collection affordability before scientific fitting. Design consistency passes independent AI review; executable source/optimizer/cost admission remains pending. FSK42's existing SGLang image and package versions were read without GPU execution. No closed family, split or budget is reopened.

## Scope and decision

Reconsider the scientific problem from the original goal, the source paper and implementation, every recorded attempt, and primary steering literature from 2022 through 2026-09-07. Produce a concrete research synthesis, a bounded definition of SOTA, and one primary next experiment whose outcome can establish behavioral improvement against the strongest applicable released methods. The existing outcome-observer implementation is paused during this reset. An already running fit-source collection may finish as an archival source packet; it does not authorize observer selection or later splits.

The paper remains empirical. Formal results belong in the appendix and must justify the implemented object. SOTA requires measured head-to-head outcomes under declared information, tuning, quality, and compute budgets. It cannot be established by a desired headline, a smaller proxy KL alone, or a passing numerical smoke test. The already running fit collection has now completed and failed its unchanged source gates; observer-v1 is closed before fitting, not merely paused. Its scientific efficacy is still unmeasured.

## Sources and precedence

1. Original goal attachments and explicit user corrections define scope.
2. Immutable raw outputs, frozen protocols, and deterministic analyses define empirical history.
3. Original paper text, author code and official OpenReview records define external claims.
4. alphaXiv discovery and prior notes locate sources; generated summaries do not establish claims.
5. Author-provided review text remains author-provided until its public provenance is found.

Use the research-paper-writing route: scientific-claim-evidence, scientific-results-and-figures, a-star-conference-paper, scientific-research-strengthening, then experimental-setup-writer for the chosen increment. Deep Research governs discovery, targeted follow-up, reconciliation and deliverable verification. The planning tool is not exposed in this session; this file is the persistent planning fallback.

## Ordered work

| Step | Status | Output |
|---|---|---|
| Reconstruct all attempts and the source-paper claim chain | complete | 176-record inventory; 97 verified historical documents; four verified aggregates; latest fit audit |
| Re-read the strongest and newest competitors from primary sources | complete within declared coverage | `literature-reset-v1.md`, raw primary extracts and verified CAST/AcT review notes |
| Synthesize the failure mechanisms and compare candidate directions | complete as research design | One intervention-response-control increment; priority and efficacy remain unverified |
| Define bounded SOTA and freeze the decisive benchmark | restricted baseline frozen; broader comparison admission pending | Baseline-v2 used the prospective48-H100-hour complete eight-candidate contract and ended technically incomplete. Its scientific conditions remain unchanged; broader comparator and later-stage cost receipts remain |
| Execute and audit the complete fixed-adapter baseline | complete; scientific entry criterion FAILED | All eight 256-question panels and final producer/base/cost joins pass native audit. Best full133 and prompt153 versus zero198; all eight lose. Close this action family before response collection. No metric/cap/product/strength rescue |
| Design a genuinely different learning-objective increment | next active dependency; no new GPU dispatch or outcome | Use the adverse harm/retention evidence and verified primary sources to distinguish short-solution imitation from base-policy-aligned outcome learning, with a strong matched adaptation comparator. Freeze the exact experiment and inclusive cost before new outcomes |
| Verify and deliver the research report and updated evidence ledgers | complete | Nine-page PDF visually checked page by page; source hashes and numerical provenance checked; receipt records the unpassed scientific gates |

The active dependency is a new learning-objective and strong-comparator research design. The complete baseline outcome audit is now adverse: no candidate is eligible for response collection. Do not repeat the broad literature reset, unchanged engine readiness, fit coverage or graders. A failed restricted candidate is neither a refutation of every representation intervention nor completion of the empirical SOTA goal.

## Current decision: complete baseline-v3 failure, 8 September

The [complete selection analysis](irc-baseline-selection-analysis-v3.md) binds all 2,048 candidate rows, the unchanged 256-row zero reference, original terminal receipts and costs. The best full candidate c05 scores133/256 against198/256 for zero, −25.390625pp with exploratory paired95%BCa [−32.03125,−19.921875]. c01 is the best prompt candidate at153/256. Every original candidate loses. All eight workers are terminal exit0, without OOM or restart; the original requirement full>198 fails. Response/control collection on this family is closed, not delayed pending a more favorable cap, score or coefficient.

The informative failure is harm to preserved competence: c05 repairs8 reference failures but harms73 successes, including69 EOS/parsed/primary-false outputs and4 caps. Its cap count falls43→8 while accuracy falls198→133. All rank/site-matched full recipes have lower online loss and worse selection accuracy than their separately trained prompt counterparts. This supports testing learning-objective/target-distribution alignment, but does not isolate its causal role or the decoding-only effect.

The next bounded step is design-only and source-grounded. Reuse `literature-reset-v1.md`, `irc-fixed-adapter-design-v1.md` and the preserved target/fit artifacts; inspect only the nearest primary methods and reusable implementations needed for the following decision. No new benchmark, training target source or rollout budget is silently admitted.

| Priority | Decision question | Required next artifact and stopping condition |
|---|---|---|
| 1 | Can base-policy-aligned outcome learning produce a useful finite intervention while retaining zero-policy successes? | One pre-outcome contract distinguishing public-solution imitation, on-policy outcome supervision and an explicitly anchored alternative. Specify target provenance, training-only information, primary quality/retention criteria and competing explanation. If no valid affordable discriminator is available, do not launch a weak proxy screen |
| 2 | Does the prospective action add anything beyond a strong matched adaptation baseline? | Verify the closest applicable all-layer ReFT, parameter-matched LoRA or reward-trained comparator from primary code, label adaptations, and equalize information/tuning/compute. Choose the appropriate comparator before outcomes; not the weakest survivor or the old restricted family |
| 3 | Can a positive initialization survive independent testing and controller comparison? | Only after the new action passes its own prospective criterion, freeze the controller/fixed/router comparison, protected utility and uncertainty. Keep exposed selection exploratory and development/sealed-test identifiers unchanged; no response collection on the failed family |

Before any new scientific execution, freeze one minimal discriminating experiment with exact code/config/data hashes, seeds, independent units, multiplicity, effect/retention thresholds, comparator and inclusive costs, failures/exclusions and success/failure/ambiguity rules. This is an untested research direction, not an already defined SOTA method. If it fails usefulness against zero, close it before controller learning; if it loses to the closest matched method, do not claim novelty or superiority. Broaden to the full three-architecture/three-construct goal only after the central effect and strongest comparison are credible.

The final measured v3 cost is15.44805799138889 H100-hours; adding the retained v2 failed/cancelled7.605374254722222 gives23.05343224611111 of48, leaving24.94656775388889. All worker lifetimes satisfy the original five-hour ceilings. These are measured terminal costs, not the old1.15/1.30 scenarios. Engineering stays separately charged; the remaining baseline allowance is not a response-stage authorization or automatic retry. The three selection CPU audits and one paired analysis reuse unchanged zero/fit evidence, with no new GPU launch this turn.

The broad empirical strength remains15/36 and EVIDENCE_THIN; S3/S5/S7 for the proposed controller contribution remain0. The narrow negative baseline audit now has complete measured effects and cost, but cannot substitute for the intended positive contribution or strongest comparator evidence. Skill route: claims → results/cost → scientific strengthening. Submission prose, skeleton, global review and compliance remain unreached; human author approval and venue/year/track disclosure are unverified. The persistent goal remains active.

## Historical execution checkpoints

The following records preserve what was known at each time. The complete selection analysis above supersedes all pending baseline-v3 outcome, producer/base and terminal-cost fields without rewriting their original receipts.

Current execution checkpoint, 8 September 06:46:41 UTC: all eight baseline-v3 workers completed their 564 updates and are generating selection answers, without OOM or restart. Completion counts are [48,48,56,32,16,104,72,64]/256, not accuracy scores. The [complete fit-component analysis](irc-baseline-fit-analysis-v3.md) verifies all 4,512 stdout/JSONL records, original 1,500 native fit targets across 12 epochs and eight finite source-conforming raw checkpoints. Its receipt-like coverage inputs are explicitly constructed; same-FSK container-to-capture time is only an upper envelope, not measured fit duration or cost. Real terminal producer receipts must still join count/order/checkpoint/base state and actual duration. Training loss decreases do not select a useful action.

The [complete zero-reference analysis](irc-baseline-zero-analysis-v3.md) and [closure](irc-baseline-zero-closure-v3.json) remain the unchanged reference: primary198/256, 43 caps and 15 EOS primary-false; all32 extraction failures overlap caps and all13 bidirectional grader disagreements remain. Complete every original 256-question candidate panel and final producer/cost join before choosing full/prompt winners. The best full must strictly exceed198 before response collection can be considered; a one-question gain is an initialization condition, not SOTA evidence. Five-hour inclusive ceilings reserved40 of40.394625745277778 baseline H100-hours available before launch; ongoing workers spend that reservation. The1.15/1.30 adapter-generation scenarios remain assumptions, not completion bounds. Reuse matching zero and fit-component hashes; do not repeatedly regrade zero, rerun unchanged coverage tests, or alter candidates, cap, metric or schedule. The separate engineering remainder2271.828642261 seconds does not authorize further unchanged readiness loops. Strength remains15/36, S5/S7=0; three architecture families, three constructs, strong comparisons and protected uncertainty/mechanism evidence remain required.

Execution update: `irc-integration-audit-v1.md` records the verified 12,500-row source reconciliation, 16 unresolved training-reference cases, upstream ReFT integration, 312 local tests and real-weight batch 1/4/8 probes. The synthetic batch-8 timing supports checking that batch on real questions; it does not freeze a scientific engine or establish a method gain. Complete scoring/exposure admission and the real-question runtime packet next. No old gate or new success threshold has changed.

Subsequent execution update: `irc-data-and-cuda-audit-v1.md` and `irc-cuda-runtime-check-v1.json` supersede those open measurement/data/CUDA prerequisites at their new hashes, without changing the earlier receipt. The prospective author-score amendment admits 7,496 training and all 5,000 test references; the fixed Math-Verify sensitivity support is separately recorded. An immutable manifest audits 144 historical sources and fixes fit 1,500, response 512, selection 256, development 256 and sealed test 4,359. An independent critic verified source identities, exclusion and allocation; unavailable old prompts and nonlexical/pretraining overlap remain outside the guarantee. The native Torch 2.13 engine failed actual-weight identity/split replay and is rejected. The isolated Torch 2.9.1/Transformers 5.16.1 environment with a narrow Pyvene overlay passes seven CUDA ReFT tests and the unchanged real-weight branch check. The local suite now passes 341 tests. Next freeze the real-question runtime and exact baseline training recipe, then fit/select a useful action before response collection. Do not rerun these passed source/allocation and synthetic gates with unchanged inputs. C67, full-rollout behavior, strong-comparator and broader-paper gates remain unpassed.

## Research questions

- Which prior failures refute a scientific premise, and which only expose an implementation or evaluator defect?
- Does the original detection axis identify a causal control variable, or a correlate of correctness, answer encoding, response length or completed reasoning?
- Which published method already owns the proposed objective, information access, conditioning or control mechanism?
- What behavior is valuable enough to optimize, and what independent measurement detects actual success?
- Which simple strong baseline would explain a putative gain, and what matched experiment rules that explanation out?
- What is the smallest experiment that can establish useful behavior improvement before expensive geometry or multi-model expansion?

## Search and stopping policy

Use one broad alphaXiv discovery pass, merge with the existing corpus, then spend the remaining targeted search on material gaps. Read the decisive method, theorem, experimental and limitation passages; inspect released code and public review/rebuttal threads when available. Record inaccessible or unpublished review records explicitly. Stop expanding a comparison family when objective, information access, mechanism and benchmark axes are covered and another query is unlikely to change the decision.

No previous failed route is erased. Old exposed data stays exploratory. The reset can revise the next scientific question, but cannot change the interpretation or thresholds of an already observed frozen experiment.

## Current execution decision, 2026-09-07

`irc-real-runtime-audit-v1.md` supersedes the pending real-question status with an explicit incomplete result: 56/64 scored rows saved, three complete 512-step profiles, and full-budget exit124 with only 8/16 questions observed. Independent hash/token/join checks pass for the saved packet; the overall runtime gate does not. Batch 8 reaches 290.84 reference tokens/s on the short workload, whereas the saved long batch reaches 91.17; no complete long-workload estimate exists. The four containers cost 0.498156 H100-hours, including the timeout. Do not start the adapter grid from the short-profile speed estimate.

The installed masked SDPA path disables GQA and duplicates KV heads. A nine-cell synthetic operator diagnostic measures about 1.58 ms and 1 GiB extra allocation at length 8,192 versus 0.099 ms without a mask; this is an operator bottleneck, not a full-model speedup. A failed fixed-tolerance numerical check remains recorded. Next qualify the existing mask-preserving GQA/FlexAttention callable, including compilation, masking, EOS and branch conformance, under the remaining unchanged runtime ceiling. Do not drop masks, shorten the scientific cap, replace questions or overwrite the failed packet. Runtime qualification is the single active step.

`irc-fixed-adapter-design-v1.md` corrects the baseline source contract: ReFT's arithmetic recipe is 12 epochs/all layers, not the one-epoch CLI default; the minimal single-layer family is not the strongest overall comparator. The source-only audit confirms all 1,500 fit examples fit within 2,690 tokens with exact native prefix and answer preservation. Exact fitting, position masking, compute qualification and selection remain downstream; no new adapter, controller, SOTA result or submission-ready paper exists.

Latest checkpoint: `irc-flex-operator-audit-v2.md` retains the native BF16 prefill failure and the exact-input FP32 control, without changing numerical tolerances. `irc-mixed-runtime-audit-v3.md` records the separately named backend's complete16-question short profile and314 passing independent integrity assertions. Its full8192 condition was launched after that gate, then SSH access failed after a worker progress report of8 completed rows. The original900-second deadline remains on the detached job; terminal status and the final output packet have not been retrieved. Runtime result retrieval/audit is the single active step, not another literature reset or an invitation to duplicate the worker. Native backward dispatch is unconfirmed and must be resolved by exact container name first.

After authentication restores access, retrieve and audit the existing packet. If complete, qualify native backward, real-weight fit cost and the explicit position/loss contracts before committing the eight-H100-hour baseline schedule. Use resident parallel workers and source-only length-aware batching where verified; all fitting, tuning, controls and failed work count. The source-only trainer audit has identified reusable upstream loss/collator code and risks from automatic EOS addition and truncation. No new training or selection recipe is yet admitted.

## Recovered full packet and current next action

Access recovered without changing or duplicating the scientific worker. `irc-mixed-full-result-audit-v3.json` verifies all16 original questions and38 source hashes. The worker exited0 without OOM in660.672601913 seconds;8 outputs ended correctly and8 hit the unchanged8192 cap. Reference throughput is159.3645 emitted tokens/s, versus154.8816 per container second. These are one engineering execution's accounting denominators, not a model-speedup or MATH test result. An independent critic found no material integrity defect.

The earlier unconfirmed backward dispatch did not create a container. After exact-name verification, the unchanged synthetic test passed on H100 in31.403719810 seconds. `irc-native-flex-backward-result-v1.json` records its one-ensemble FP64 output/VJP comparison. Known measured engineering use is about0.78151 H100-hours, plus a separate0.10-hour historical-overhead reserve.

That fit probe is now complete: all 18 optimizer steps and source/ownership checks pass. All 1,500 exact solutions remain; omitting only post-EOS template whitespace yields 507,475 total and 356,743 target tokens per epoch. No diagnostic checkpoint is baseline-eligible. The completed source-cost analysis makes the eight-H100-hour fit/selection schedule budget-incomplete, not scientifically refuted.

The subsequent profile has usable CUDA evidence, but static compilation fails the predeclared scalar trajectory comparison on ten of18 steps. It stays failed. Separate FP64 and native accumulation/raw-checkpoint contracts now pass; the latter uses four unique questions and two updates, not a useful baseline. The full restricted baseline is now running on eight FSK GPUs under config207e37dd9a7bf977378db65e41db53177451ee36a037e8d347beaa99c34a9acf. The active dependency is complete fit/selection coverage and its outcome audit, not another engineering probe.

The explicit `irc-baseline-resource-amendment-v2.md` replaces the assistant-origin baseline allowance of eight H100-hours with 48 before eligible checkpoint or selection outcomes. All eight candidates, 1,500/256 questions, prompt-only/zero controls and cap remain; positive strength is fixed at the learned adapter's native unit strength, with only final 12-epoch checkpoints evaluated. Original engineering and later-stage ceilings are unchanged, and their work is not reassigned. A useful full-generation action, S5/S7, response-control efficacy, strongest competitors and the full three-family/three-construct goal remain unpassed. This is research progress, not completion or an external-access blocker.

## Superseding terminal checkpoint: baseline-v2 technical abort

`irc-baseline-technical-abort-audit-v2.md` replaces the preceding running status. All eight original workers are terminal. c01/c02 naturally fail on FP32 short-prefill FlexDecoding kernel resources; six other workers are administratively stopped after the recorded abort decision. Three final12-epoch raw adapters remain archival only. All48 output files and four natural-failure logs are copied and hash-verified. The stage costs7.605374254722222 H100-hours including every failed and cancelled lifetime. There is no selected action or scientific efficacy result.

The deeper failure analysis locates a missing shape-coverage gate: eight of32 source-only selection batches have Q<128; saved zero prefixes and the three completed zero shares align exactly with that compiler boundary. This happens before adapter construction and does not compare prompt versus full intervention. All1,692 surviving fit updates and exact native targets pass a separate CPU archival audit; lower training loss does not admit a useful model. The incomplete outcome analyzer correctly blocks before ranking or regrading partial results. An independent critic verifies352 archival/lineage assertions without correctness access.

The next single dependency is a separately declared inference-engine qualification, not another literature reset, a shortened cap, a reduced candidate family or response collection. Source-only discovery identifies the existing HF kernel-options forwarding and Torch regular-Flex switch; no repair has been implemented or GPU-qualified. An explicit engineering resource decision is required because only9.198053592 seconds remain under the unchanged original engineering allowance. The unused baseline allowance is not silently reassigned. Pending that decision, preserve the archives and existing research design; the goal, C67, S5/S7 and SOTA remain open.

## Superseding checkpoint: short-prefill v4 numerical failure

The autonomous FSK authority supported a separately recorded additional one-H100-hour engineering increment; it was not a transfer or a user-approved numerical budget. The v4 alias reuses the original numerical wrapper and changes only the native short-prefill dispatch option. Its exact server CPU preflight passes18 tests with one CUDA skip. Local full-suite475 pass/one skip is a CPU regression result, not CUDA admission.

`irc-short-prefill-failure-audit-v4.md` records the actual first gate: Q113 and Q2 pass all five comparisons, then Q93 fails a K/V gradient comparison. The emitted error does not identify K versus V. Source order implies both forwards and dQ passed at Q93; this does not justify forward-only admission. All32 remaining geometries, actual-weight prefills and complete runtime gates stay unrun. Cost46.528479489 seconds leaves3553.471520511 additional-allocation seconds unspent; the old engineering9.198053592 seconds and baseline cost remain unchanged.

The complete342-file archive is hash-verified. A separately generated CPU FP64 oracle-algebra check agrees with native SDPA within1.60e-14, without reproducing the failed GPU tensors. No Python-level mutation defect is established. The symbolic backward and reverse Q-block metadata are concrete diagnostic candidates, not diagnosed causes. A similarly titled upstream report concerns a different mask operation and is not accepted as a fix.

The next useful increment is a source-bound counterfactual diagnostic of freshQ93 versus the frozen113→2→93 shape history, with labeled comparisons, preserved failing tensors and both forward/reverse sparse metadata. Its exact resource/decision contract must be frozen separately; no such GPU dispatch occurred in this checkpoint. Do not reopen the baseline, narrow its family, rerun the literature survey or treat this engineering failure as an IRC efficacy result. The strengthened scientific design and full-paper target remain in force.

## Superseding checkpoint: v5 history counterfactual completed

`irc-short-prefill-diagnostic-audit-v5.md` records the completed two-process comparison: freshQ93 passes; identical Q93 tensors after113→2→93 fail the named K-gradient assertion. Nineteen of20 planned native comparisons were attempted,18 pass and one fails; the following V assertion was not reached. A separately labeled native-image CPU reconstruction also finds the saved V gradient wrong. Actual tensor/reference/VJP hashes and metadata match across arms, but saved compressed block counts differ and violate their single-block bounds.

The generated symbolic mask source has128 reduction lanes without the actual length1 bound. CPU address enumeration exposes beyond-logical-tensor reads; this is a source-level defect, not a measured repair or proof of sole runtime cause. The next decisive increment should change only native mask-construction compilation and capture metadata before and after use, leaving attention math/precision/history fixed. No generated-kernel patch, dependency change, reduced baseline or full model run is authorized by this completed diagnostic.

The first Docker create failed from an erroneous output path before any container existed. Its original no-retry record remains; an explicit reactive pre-execution amendment, independently checked against actual argv/mounts, preceded the two successful dispatches. Both corrected containers are terminal after77.908420112 total seconds; the additional engineering allocation has3475.563100399 seconds unspent.495 native/compiler files are hash-verified. C83 remains contradicted; C67/S5/S7/SOTA and the full paper remain unpassed. The goal stays active.

## Superseding checkpoint: v6 mask-only comparison and original matrix pass

`irc-mask-construction-audit-v6.md` records a successful prerequisite: automatic construction has malformed Q2/Q93 counts immediately after builder return, unchanged afterward; eager native construction passes all three paired cells with valid stable metadata. Actual inputs/VJPs/references match. The A/B native denominator is 29/30 attempted, 28 pass, one K failure and one following V unattempted. A separate saved-V CPU failure does not alter it. Sole causal responsibility is not established.

After independent actual-packet review and prospective C admission, the observer-free eager process passes all original 35 ordered shapes and 175 source-mapped numerical comparisons, plus the helper's predicate/finite/zero checks. C does not audit every compressed metadata array. Total v6 cost is 155.895285388 H100 container-seconds; the additional engineering allocation has 3319.667815011 seconds remaining, with no baseline transfer. Both complete archives match their native inventories. No new scientific labels, model outputs or sealed data were exposed.

The next single dependency is a separately frozen minimal eager-mask engine integration followed by the original actual-weight B/C contracts: all32 production-prefill batches, then the16 fit-only full-runtime questions at8,192 tokens. Reuse passed source/allocation/oracle gates when their contract and effective call path remain unchanged; do not rerun a broad literature reset. A changed effective call path requires corresponding qualification, not inherited success by name. Preserve original v4 and baseline failures. Only after engine admission may a complete eight-candidate baseline be separately opened and parallelized; no survivor shortcut or response collection yet. Full three-architecture/three-construct empirical SOTA remains the goal, not an inference from this engineering pass.

## Superseding checkpoint: v7 source failure and complete v8 inference qualification

v7 failed before any GPU stage. The first native CPU failure exposed an invalid model-path interpolation; its recorded amendment did not hide the second failure, where both the producer and local fixture used the wrong hash serialization. Independent reconstruction matches all32 original canonical-list input/mask digests and0/32 raw-byte digests. The lesson is to exercise the actual producer against immutable external-contract fixtures before GPU, not to trust a green suite whose expected values duplicate the new implementation. Both failures remain, costing zeroGPU seconds.

The separately frozen v8 repair reuses the existing serializer without changing source rows, tokenizer, prompt, attention arithmetic, scorer, cap or tolerance. Native CPU105pass/oneCUDA skip precedes a complete one-shot A→B→profile→full sequence. The isolated registry passes35 ordered geometries/175 source-mapped comparisons, all32 actual-weight source prefills and both16-row runtime stages. Full output has14EOS and2caps,13primary successes and14sensitivity successes. The one day-name formatting disagreement remains a primary failure. All16 full prefixes match the512 profile tokens in an explicitly post hoc diagnostic; this is not full-length replay/intervention evidence.

`irc-eager-engine-result-v8.json` binds952.411169589 inclusive H100-seconds and a verified721-file archive. The first permission-denied tar is preserved; the complete archive reads the existing copied caches asroot without changing permissions or rerunning GPU work. The additional engineering allowance retains2367.256645422seconds; baseline-v2's7.605374254722222hours remain charged separately. The v3/v8 question sets share one row, so neither timing nor correctness counts imply a speedup or quality change.

The next single technical dependency is a bounded static-compilation/eager-mask composition bridge at the original training buckets and accumulated/raw-checkpoint path. Existing old-mask static and new-mask dynamic receipts are useful but do not prove this new composition. Reuse their exact oracle, target, optimizer and checkpoint contracts; do not rerun unchanged source/allocation, numerical or literature gates. Then make the one needed baseline registration change and freeze all eight fresh candidates plus a fresh complete shared zero. No survivor reuse, incomplete family, changed seed/metric/cap/product or response collection is admitted.

Reserve comparisons and analysis for complete packets. An eight-worker five-hour inclusive schedule would fit40hours inside the remaining40.394625745277778-hour baseline allowance, but source-bound end-to-end cost analysis must precede launch. Dense generation currently spends65419 of131072 advance-step row slots after row absorption; this is a scheduling diagnostic, not measured wasted FLOPs, and does not authorize changing the frozen batching after exposure. Report primary/sensitivity disagreements, EOS/cap rates and format-related gains separately when complete baseline outputs become available; they cannot replace the unchanged primary selection rule. Scientific strength remains15/36, with C67/S5/S7, strong comparators and the full three-family/three-construct SOTA objective unpassed.
