# Exact-source eager engine qualification v8

## Why a successor exists

v7 is closed as a failed native CPU admission, not a failed or passed GPU experiment. Its complete source archive and both failed configurations remain immutable. It used zero GPU seconds and 26.72699762135744 CPU-container wall seconds. The path correction was explicitly amended and recorded. No second correction or GPU launch occurred within v7.

The isolated native mask implementation and original attention/oracle remain unchanged. The newly established software defect is in the input-check serializer: the frozen v4 inventory uses payload_sha256(tensor.tolist()), but the v7 probe and its local expected-value fixture used raw NumPy bytes. Independent tokenizer-only reconstruction matches every canonical-list array digest and every prompt hash, length, width and identity across all32 batches; raw-byte hashes match none. No model outcomes informed this repair.

The research-paper-writing router reopens claim/result/strength and experimental-design gates at this exact source-contract defect. This is a bounded prerequisite to the complete empirical steering comparison, not a renewed literature search or a novelty claim. The three-architecture/three-construct objective, applicable strong baselines, uncertainty, stratification and mechanism/protection requirements remain unchanged and unsatisfied.

## Discovery and minimal repair

Reuse existing materialize_outcome_score_data.payload_sha256 on the token and mask lists. Delete the unused hashlib import. Make the expected-value fixture use the original inventory convention and observe the positive first-forward conformance tests fail before changing the producer. Do not edit the frozen inventory, tokenizer, dataset, prompt construction, native mask binding, precision, attention kernel, sampling, grading, caps, model, geometry order or FP64 oracle.

The actual first-forward kwargs/no-sampling and corruption tests remain. Before GPU, require the exact native source/version checks, all32 inventory matches through the repaired producer stopping at a CPU-only forward boundary, the exact16 fit packet validation, and the native CPU test suite. This real frozen-source regression is authoritative for serialization compatibility; a fixture reproducing the producer is insufficient.

CPU-only test failures are software-development evidence and must be preserved. They do not create permission to relax any GPU gate. Unlike v7's overbroad container wording, the no-retry commitment here applies unambiguously to each GPU stage: after its first GPU container starts, there is no packaging, numerical, compiler, resource or timeout retry in this increment. GPU execution requires independent final source/argv review and a freshly idle authorized H100.

## Immutable workloads and resources

Reuse the original35 ordered shapes and175 FP64 comparisons, original32 source-ordered selection prefills, and the original16 short-prompt fit-only questions from v4. The A helper's source/code and tolerances stay fixed; fresh namespace call sites bind to the exact new registry callables. B loads the pinned actual Qwen3-8B weights and executes only the production first forward, with no sampled/scored answer. C uses the original runtime, graders and branch/action/cache checks.

Reserve 3300 GPU-container seconds from the unchanged3319.667815011-second additional-engineering remainder. v7 spent zero from it. This is not new funding and not a transfer from the old9.198053592-second engineering remainder or baseline-v2's7.605374254722222 H100-hours. CPU-only development/transport wall time is reported separately, never as H100 occupancy.

| Stage | Contract | Inclusive ceiling | Child timeout |
|---|---|---:|---:|
| A | Original35/175, isolated registry, no observer | 300s | 260s |
| B | All32 actual-weight prefills, no generation/scoring | 600s | 560s |
| C-profile | Same16 fit questions, batch8,512steps, original checks | 400s | 360s |
| C-full | Same16 fit questions, batch8,8192steps, complete raw/scored rows | 2000s | 1960s |

Ten seconds kill grace and thirty seconds startup/shutdown margin per stage fit the inclusive ceilings. Use one verified-idle FSK42 H100, separate fresh caches, named containers and never-existing outputs, pinned image/runtime/read-only model mounts. No other process is stopped. An SSH observation timeout is not a container terminal and never warrants a duplicate launch.

Every preceding stage must pass completely, with integrity review, before the next is dispatched. A missing/failing cell closes later GPU stages. Do not rescue archived survivors, reduce C generation cap or select correct/easy questions. Fit outcomes are engineering data; incorrect or cap-failed answers remain reported but do not by themselves change the technical pass criterion. No sealed data or fitted steering candidate is evaluated.

## Interpretation and handoff

A complete pass supports only a separately audited engine-admission decision. A complete eight-candidate scientific baseline still requires its own successor source/selection/resource contract. No engine-only result establishes SOTA, effectiveness, multi-architecture validity or general compiler correctness. Assertion counts are not independent replicates and do not support effect confidence intervals.

Current scientific maturity remains EVIDENCE_AVAILABLE / EVIDENCE_THIN with S1–S9=3,2,0,3,0,0,0,4,3 (15/36), presentation3,3,4. All prose/fair-strong/section/global/submission gates remain blocked. Responsible-author verification, AI disclosure classification and venue compliance are unverified. Preserve source/config/argv, all CPU and GPU failures, full raw artifacts, independent critic scope, actual costs and assistance provenance.
