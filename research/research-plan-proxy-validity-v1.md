# Teacher-forced proxy validity research plan, version 1

Freeze date: 2026-09-07. Status: active after Flow-step support version 1 closure and frontier triage.

1. Preserve every prior route closure, raw receipt, invalid attempt, and unopened allocation.
2. Add ASC and W2S-Multi to the frontier and block another time-support, adaptive-gating, KL-scale, or generic optimal-control successor.
3. Freeze the independent proxy-validity estimand, 12-concept allocation, 14 conditions, generation settings, local judge, correlation metrics, uncertainty, and stopping rule before output.
4. Reuse the released FLAS generator, exact condition specifications, upstream judge prompts/parser, existing local Qwen proxy pattern, hash utilities, and bootstrap conventions. Add only generation state cleanup, shard receipts, exact joins, and the frozen analyzer.
5. Pass deterministic synthetic tests and one 14-output GPU smoke.
6. Run six two-concept generation shards on six H100s, then six matching Qwen3-14B judge shards, each model loaded once per shard.
7. Run the frozen analyzer before inspecting condition-level generation scores. If any hypothesis fails, retire teacher-forced likelihood as a primary method screen and require behavior-first successors.
8. Do not open the fresh 24 concepts or design another method until the instrument decision is recorded.

This plan accelerates research by testing the screen itself before spending another cycle optimizing against it. It cannot rescue any observed Flow-step support condition or create a behavior, novelty, or SOTA result.
