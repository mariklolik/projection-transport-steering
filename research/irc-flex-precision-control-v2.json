{
  "schema": "irc-flex-precision-control-v2",
  "created_at": "2026-09-07 15:46:37 UTC",
  "record_timing": "Before these paired controls, after observing unchanged-tolerance bfloat16 Flex prefill failure. No actual-weight Flex outputs.",
  "question": "Does changing only the attention arithmetic implementation/precision on the identical BF16 inputs remove the observed pointwise prefill error?",
  "unchanged_inputs": "Same seed, bf16 draws, shapes, mask, scaling, grouped FP64 oracle, zero convention, tolerances and B,Q,H,D output layout as supplemental prefill.",
  "comparators": [
    "Native installed SDPA default bf16",
    "Native installed SDPA MATH bf16",
    "Native Flex on losslessly upcast bf16 inputs with FLOAT32_PRECISION=ieee, output cast back tobf16"
  ],
  "inferences": "Diagnoses precision sensitivity; no retroactive gate pass, exact error-source attribution, new backend admission or model speedup.",
  "prior_operator_seconds": 93.22350094,
  "deadline_seconds": 80,
  "kill_grace_seconds": 20,
  "maximum_cumulative_operator_seconds": 193.22350094,
  "original_operator_envelope_seconds": 200,
  "code": "import hashlib, json, time\nfrom pathlib import Path\nimport torch\nfrom transformers.integrations import flex_attention as flex\nfrom transformers import masking_utils as masks\ntorch.set_num_threads(2)\ntorch.set_num_interop_threads(1)\ntorch.manual_seed(20260907)\nsources={name:hashlib.sha256(Path(source.__file__).read_bytes()).hexdigest() for name,source in ((\"flex\",flex),(\"masking\",masks))}\nassert sources=={\"flex\":\"46655f4b8b528a75cdaa418e2fc6690fa4c2dd5e0180fe78900f4db0a7c83650\",\"masking\":\"c159cd91c2a7fcafce04a8b6cbca55c320ce904b8ebf634383c97da5d9313ce3\"}\nprint(json.dumps({\"event\":\"identity\",\"source_sha256\":sources,\"torch\":torch.__version__}),flush=True)\nwith torch.no_grad():\n    length=129\n    query=torch.randn(8,32,length,128,device=\"cuda\",dtype=torch.bfloat16)\n    key=torch.randn(8,8,length,128,device=\"cuda\",dtype=torch.bfloat16)\n    value=torch.randn_like(key)\n    mask=torch.ones(8,length,device=\"cuda\",dtype=torch.bool)\n    for index in range(8):\n        mask[index,:index]=False\n        if index%2==0:\n            mask[index,-index-1:]=False\n\n    from contextlib import nullcontext\n    from types import SimpleNamespace\n    from torch.nn.attention import SDPBackend, sdpa_kernel\n    from transformers.integrations import sdpa_attention as sdpa\n    module=SimpleNamespace(num_key_value_groups=4,is_causal=True,training=False)\n    input_hashes={name:hashlib.sha256(tensor.contiguous().view(torch.uint8).cpu().numpy().tobytes()).hexdigest() for name,tensor in ((\"query\",query),(\"key\",key),(\"value\",value),(\"mask\",mask))}\n    ids=torch.arange(length,device=\"cuda\")\n    keep=(ids[None,:,None]>=ids[None,None,:]) & mask[:,None,:]\n    scores=torch.einsum(\"bghqd,bgkd->bghqk\",query.reshape(8,8,4,length,128).double(),key.double())*128**-0.5\n    scores.masked_fill_(~keep[:,None,None,:,:],-torch.inf)\n    valid=keep.any(-1)\n    weights=torch.where(valid[:,None,None,:,None],scores.softmax(-1),torch.zeros_like(scores))\n    oracle=torch.einsum(\"bghqk,bgkd->bghqd\",weights,value.double()).reshape(8,32,length,128).transpose(1,2)\n    print(json.dumps({\"event\":\"paired_input\",\"sha256\":input_hashes,\"fully_masked_queries\":int((~valid).sum())}),flush=True)\n    for name in (\"sdpa_default_bfloat16\",\"sdpa_math_bfloat16\",\"flex_fp32_ieee_cast_bfloat16\"):\n        torch.cuda.synchronize()\n        tick=time.perf_counter()\n        with sdpa_kernel(SDPBackend.MATH) if name==\"sdpa_math_bfloat16\" else nullcontext():\n            if name==\"flex_fp32_ieee_cast_bfloat16\":\n                block=masks.flex_attention_mask(8,length,length,attention_mask=mask,device=\"cuda\")\n                output=flex.flex_attention_forward(module,query.float(),key.float(),value.float(),block,scaling=128**-0.5,kernel_options={\"FLOAT32_PRECISION\":\"ieee\"})[0].to(query.dtype)\n            else:\n                output=sdpa.sdpa_attention_forward(module,query,key,value,keep[:,None],scaling=128**-0.5)[0]\n        torch.cuda.synchronize()\n        seconds=time.perf_counter()-tick\n        error=None\n        try:\n            torch.testing.assert_close(output.double(),oracle,rtol=0.005,atol=0.0005)\n        except AssertionError as failure:\n            error=str(failure)\n        difference=(output.double()-oracle).abs()\n        violates=difference>(0.0005+0.005*oracle.abs())\n        row={\"event\":\"precision_control\",\"implementation\":name,\"oracle_error\":error,\"mismatched_elements\":int(violates.sum()),\"total_elements\":output.numel(),\"max_abs_fp64_error\":float(difference.max()),\"all_finite\":bool(torch.isfinite(output).all()),\"fully_masked_output_exact_zero\":bool(torch.all(output[~valid]==0)),\"cold_call_seconds\":seconds,\"output_dtype\":str(output.dtype)}\n        print(json.dumps(row),flush=True)\nprint(json.dumps({\"event\":\"complete\",\"cells\":3,\"model_outcomes\":0,\"old_flex_failure_overridden\":False}),flush=True)\n",
  "command": "/usr/bin/ssh -o BatchMode=yes -o ConnectTimeout=10 -o ControlPath=none -o ControlMaster=no avi-gn-fsk42 'sudo -n docker run -d --name pts-irc-flex-precision-control-v2 --network none --gpus device=2 --user 11027:11027 --mount type=bind,src=/home/mekashirskiy/projection-transport-steering/tmp/irc-real-runtime-v1,dst=/work,readonly --mount type=bind,src=/home/mekashirskiy/projection-transport-steering/tmp/irc-pyvene-oldruntime-v1/packages,dst=/overlay,readonly --mount type=bind,src=/home/mekashirskiy/projection-transport-steering/.venv,dst=/ptsenv,readonly --env PYTHONPATH=/work/src:/overlay:/ptsenv/lib/python3.12/site-packages --env MPLBACKEND=Agg --env PYTHONDONTWRITEBYTECODE=1 --env OMP_NUM_THREADS=2 --env OPENBLAS_NUM_THREADS=2 --env MKL_NUM_THREADS=2 --env TRITON_CACHE_DIR=/tmp/irc-triton --env MPLCONFIGDIR=/tmp/irc-matplotlib --env USER=mekashirskiy --env TORCHINDUCTOR_CACHE_DIR=/tmp/irc-inductor --env TORCHINDUCTOR_COMPILE_THREADS=2 --env CUDA_CACHE_PATH=/tmp/irc-cuda-cache --entrypoint timeout sha256:edc40b8cd5d95f053f05b0f7807e256038cd604c08aee981535c8e44d72180cb --signal=TERM --kill-after=20s 80s python3 -S -c '\"'\"'import hashlib, json, time\nfrom pathlib import Path\nimport torch\nfrom transformers.integrations import flex_attention as flex\nfrom transformers import masking_utils as masks\ntorch.set_num_threads(2)\ntorch.set_num_interop_threads(1)\ntorch.manual_seed(20260907)\nsources={name:hashlib.sha256(Path(source.__file__).read_bytes()).hexdigest() for name,source in ((\"flex\",flex),(\"masking\",masks))}\nassert sources=={\"flex\":\"46655f4b8b528a75cdaa418e2fc6690fa4c2dd5e0180fe78900f4db0a7c83650\",\"masking\":\"c159cd91c2a7fcafce04a8b6cbca55c320ce904b8ebf634383c97da5d9313ce3\"}\nprint(json.dumps({\"event\":\"identity\",\"source_sha256\":sources,\"torch\":torch.__version__}),flush=True)\nwith torch.no_grad():\n    length=129\n    query=torch.randn(8,32,length,128,device=\"cuda\",dtype=torch.bfloat16)\n    key=torch.randn(8,8,length,128,device=\"cuda\",dtype=torch.bfloat16)\n    value=torch.randn_like(key)\n    mask=torch.ones(8,length,device=\"cuda\",dtype=torch.bool)\n    for index in range(8):\n        mask[index,:index]=False\n        if index%2==0:\n            mask[index,-index-1:]=False\n\n    from contextlib import nullcontext\n    from types import SimpleNamespace\n    from torch.nn.attention import SDPBackend, sdpa_kernel\n    from transformers.integrations import sdpa_attention as sdpa\n    module=SimpleNamespace(num_key_value_groups=4,is_causal=True,training=False)\n    input_hashes={name:hashlib.sha256(tensor.contiguous().view(torch.uint8).cpu().numpy().tobytes()).hexdigest() for name,tensor in ((\"query\",query),(\"key\",key),(\"value\",value),(\"mask\",mask))}\n    ids=torch.arange(length,device=\"cuda\")\n    keep=(ids[None,:,None]>=ids[None,None,:]) & mask[:,None,:]\n    scores=torch.einsum(\"bghqd,bgkd->bghqk\",query.reshape(8,8,4,length,128).double(),key.double())*128**-0.5\n    scores.masked_fill_(~keep[:,None,None,:,:],-torch.inf)\n    valid=keep.any(-1)\n    weights=torch.where(valid[:,None,None,:,None],scores.softmax(-1),torch.zeros_like(scores))\n    oracle=torch.einsum(\"bghqk,bgkd->bghqd\",weights,value.double()).reshape(8,32,length,128).transpose(1,2)\n    print(json.dumps({\"event\":\"paired_input\",\"sha256\":input_hashes,\"fully_masked_queries\":int((~valid).sum())}),flush=True)\n    for name in (\"sdpa_default_bfloat16\",\"sdpa_math_bfloat16\",\"flex_fp32_ieee_cast_bfloat16\"):\n        torch.cuda.synchronize()\n        tick=time.perf_counter()\n        with sdpa_kernel(SDPBackend.MATH) if name==\"sdpa_math_bfloat16\" else nullcontext():\n            if name==\"flex_fp32_ieee_cast_bfloat16\":\n                block=masks.flex_attention_mask(8,length,length,attention_mask=mask,device=\"cuda\")\n                output=flex.flex_attention_forward(module,query.float(),key.float(),value.float(),block,scaling=128**-0.5,kernel_options={\"FLOAT32_PRECISION\":\"ieee\"})[0].to(query.dtype)\n            else:\n                output=sdpa.sdpa_attention_forward(module,query,key,value,keep[:,None],scaling=128**-0.5)[0]\n        torch.cuda.synchronize()\n        seconds=time.perf_counter()-tick\n        error=None\n        try:\n            torch.testing.assert_close(output.double(),oracle,rtol=0.005,atol=0.0005)\n        except AssertionError as failure:\n            error=str(failure)\n        difference=(output.double()-oracle).abs()\n        violates=difference>(0.0005+0.005*oracle.abs())\n        row={\"event\":\"precision_control\",\"implementation\":name,\"oracle_error\":error,\"mismatched_elements\":int(violates.sum()),\"total_elements\":output.numel(),\"max_abs_fp64_error\":float(difference.max()),\"all_finite\":bool(torch.isfinite(output).all()),\"fully_masked_output_exact_zero\":bool(torch.all(output[~valid]==0)),\"cold_call_seconds\":seconds,\"output_dtype\":str(output.dtype)}\n        print(json.dumps(row),flush=True)\nprint(json.dumps({\"event\":\"complete\",\"cells\":3,\"model_outcomes\":0,\"old_flex_failure_overridden\":False}),flush=True)\n'\"'\"''",
  "baseline_fitting_authorized": false
}
